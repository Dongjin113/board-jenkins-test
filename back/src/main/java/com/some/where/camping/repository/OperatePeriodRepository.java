package com.some.where.camping.repository;

import com.some.where.domain.camping.OperatePeriod;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OperatePeriodRepository extends JpaRepository<OperatePeriod, Long> {
}
